// domain 모듈 — 순수 도메인 모델과 Repository 인터페이스만 존재한다.
//
// 이 모듈은 Spring, JPA 등 어떤 외부 프레임워크에도 의존하지 않는다.
// 덕분에 infra/lib/app 모듈이 교체되더라도 domain은 변경 없이 유지된다.
// (클린 아키텍처의 핵심: 가장 안쪽 레이어는 바깥에 의존하지 않는다)
//
// 루트 build.gradle.kts의 subprojects 블록에서 공통 의존성을 이미 상속받으므로
// 이 파일에 별도로 추가할 의존성이 없다.
